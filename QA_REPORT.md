# CRYPTORA — визуальный QA-проход: отчёт A–Z

Ветка `arena/01a0aeee-cryptora`, коммит **`6dced33`** (`fix(ui): визуальный QA-проход — оверлап шапки, дубль цены, терминология`).
Продакшн не трогался; все работы — только в репозитории.

---

## A. Код на проде vs код в ветке

**Вывод: скриншот прода отражает код ДО правок, а не «отвалившийся» фикс.**

| Проверка | Результат |
|---|---|
| `origin/main` | `ed3b4f2` (merge `161511d`) |
| `git merge-base --is-ancestor 1be5461 origin/main` | **true** |
| `git merge-base --is-ancestor 161511d origin/main` | **true** |

Оба UI-коммита уже в `main`. Значит перекрывающийся заголовок на скриншоте — это код, развёрнутый на VPS **до** мержа, либо сам VPS не перезадеплоен после `ed3b4f2`. Развёртывание на VPS вручную и мной не выполнялось (по вашему требованию).

**Прод осмотреть из песочницы невозможно** — проверено, не предполагается:
- DNS `cryptora.duckdns.org` → `89.125.24.50`, TCP-соединение устанавливается, TLS → `SSL_ERROR_SYSCALL`, HTTP-код `000`;
- исходящий трафик разрешён только к `registry.npmjs.org`, `github.com`, `api.github.com`, `codeload.github.com`, `pypi.org`. `api.binance.com` → `000`.

Поэтому весь QA ниже выполнен **против кода ветки**, как вы и просили.

---

## B. Шапка — корень проблемы

`src/components/layout/Header.tsx`. Строка: `mx-auto flex min-w-0 max-w-[1920px] flex-wrap items-center gap-x-2`, три ребёнка: бренд `order-1 … shrink-0` (:392), `<nav>` `order-3 … xl:flex-1 xl:justify-center` (:436), контролы `order-2 ml-auto … shrink-0` (:479).

Каждый пункт навигации был `relative flex shrink-0 items-center … whitespace-nowrap` (ссылка :215, обёртка дропдауна :277, кнопка :283) — **без `truncate`, и `<nav>` без `overflow`**.

Несжимаемый (`shrink-0`) неразрывный (`whitespace-nowrap`) элемент физически не может уложиться в отведённый `flex-1`. Когда содержимое шире контейнера, оно выплёскивается наружу; `justify-center` делает выплёск **симметричным** — влево под бренд и вправо под контролы. Отсюда `CRYPTORA` наезжает на `Обзор` именно при сужении окна.

`overflow-hidden` на `<nav>` неприменим: внутри живут выпадающие меню.

## C. Исправление шапки

Архитектурное, а не `margin-left` под один экран — 5 правок:

| Узел | Было | Стало |
|---|---|---|
| пункт-ссылка (:215) | `shrink-0 … whitespace-nowrap` | `min-w-0 shrink …` + `<span class="truncate">` |
| обёртка дропдауна (:277) | `shrink-0` | `min-w-0 shrink` |
| кнопка дропдауна (:283) | `shrink-0` | `min-w-0 shrink` |
| подпись дропдауна | без усечения | `<span class="truncate">` |
| шеврон | без ограничения | `shrink-0` |

Теперь при нехватке ширины пункт **сжимается и усечается**, а не выплёскивается: бренд и контролы остаются `shrink-0`, навигация — единственная гибкая зона. Перекрытие стало структурно невозможным на любой ширине, включая 1024.

Существующий компактный режим сохранён и не менялся: `text-[13px]` → `2xl:text-sm`, иконки `hidden … nav2xl:block`, инлайн-поиск `hidden navxl:block`, бейдж версии `navxl:inline-block` (кастомные брейкпоинты `navxl:1440px`, `nav2xl:1700px`).

Бюджет ширины на 1280 px (консервативно, 7,4 px/символ Inter 13 px): навигация ≈ 586 + бренд 137 + контролы 240 + отступы 32 = **995 px < 1280**. Запас есть.

## D. Навигация — состояние

Уже корректно, дублей не создавал:
- `Рынок ▾` → `Спот` (`/market`) / `Фьючерсы` (`/futures`) — `navigation.ts`, `MARKET_NAV_ITEMS`;
- в `PRIMARY_NAV_ITEMS` **5** пунктов, отдельного top-level `Фьючерсы` нет;
- роуты `/market` и `/futures` сохранены;
- `PRIMARY_NAV_CAPACITY = 6` (`navigation.ts:84`) — только тестовая граница, **не** механизм схлопывания в рантайме.

## E. Data Mode

Карточки «Data Mode» в интерфейсе **нет** — заменять нечем и не нужно (фейковой не добавлял). KPI-ряд перестроен на реальных карточках: `lg:grid-cols-5` и ровно 5 карточек — `Total Market Cap`, `24h Volume`, `BTC Dominance`, `Fear & Greed Index`, `Market Breadth`. Пустой 6-й колонки нет (5 карточек на 5 колонок).

## F. Баннер источника данных

Уже соответствует ТЗ: `Collapsible` с `testId="overview-source-collapsible"`, **без `defaultOpen`** → свёрнут по умолчанию. Свёрнутая подпись компактная — `LIVE Data · Binance / KuCoin` (в QA-режиме `QA Data · внутренний датасет`). Развёртка содержит объяснение происхождения данных.

## G. Зачистка «перпов»

В пользовательском интерфейсе — **0 вхождений**. Остались 4 вхождения только в комментариях/тестах (`LiquidationsPage.tsx:59`, `CalendarService.ts:38,62`, `BybitLiquidationStream.ts:6`) — внутренние идентификаторы не трогал. На Overview: `24ч Futures Volume`.

## H. Терминология

Приведено к «естественный русский + нормальный торговый английский»:

| Было | Стало |
|---|---|
| `Открытый интерес`, `Открытый интерес (OI)` | `Open Interest` |
| `Фандинг` | `Funding` |
| `Объём` | `Volume` |
| `Изм. 24ч` | `24ч Change` |
| `Агрегированный OI` | `Open Interest` |
| `Суточный объём Futures` | `24ч Futures Volume` |
| `Базис BTC` | `BTC Basis` |
| `Ликвидации за 24h` | `Ликвидации · 24ч` |

Единая система статусов (§9) — убраны все составные метки:

| Было | Стало |
|---|---|
| `LIVE SPOT` / `QA-ДАТАСЕТ` (Header ×2) | `СПОТ • LIVE` / `СПОТ • QA` |
| `LIVE-ДАННЫЕ РЫНКА` (Header) | `РЫНОК • LIVE` / `РЫНОК • QA` |
| `LIVE-ТИКЕР` / `QA-ТИКЕР` | `LIVE` / `QA` |
| `LIVE · BINANCE FUTURES` | `FUTURES • LIVE` |
| `LIVE · BINANCE · {время}` | `BINANCE • LIVE · {время}` |
| `LIVE-ПЛИТКИ · BINANCE / KUCOIN` | `BINANCE / KUCOIN • LIVE` |
| `LIVE СПОТ · BINANCE / KUCOIN` (×2) | `СПОТ • LIVE` |
| `LIVE-ДЕТЕКЦИЯ` / `QA-СТРИМ` | `LIVE` / `QA` |
| `QA-СРЕЗ` / `QA-ДАТАСЕТ` (×5) | `QA` |
| `QA-СВЕЧИ` / `LIVE · BINANCE` (график) | `QA` / `BINANCE • LIVE` |
| `LIVE-СВЕЧИ 1D` / `QA-СВЕЧИ` (корреляции) | `СВЕЧИ 1D • LIVE` / `СВЕЧИ 1D • QA` |
| `LIVE Spot (Binance / KuCoin)` (tooltip ×2) | `СПОТ • LIVE (Binance / KuCoin)` |

`LIVE-FIRST` в комментариях кода и `QA-датасет` внутри описательных предложений (`MarketPage.tsx:160,342`, `OverviewPage.tsx:293,308,424`, `LiquidationsPage.tsx:475`) оставлены — это проза, а не бейджи. Внутренние идентификаторы `LIVE_STREAM` не менялись.

## I. Дубль цены на графике — корень

`src/components/common/CandleChart.tsx`. Серия свечей (:155) создавалась с **дефолтами** lightweight-charts, т.е. `lastValueVisible: true` и `priceLineVisible: true`. При этом код уже рисует текущую цену осознанно — `createPriceLine({ price: lastCandle.close, axisLabelVisible: true, lineStyle: 2 })` на :356 (путь `setData`) и :402 (realtime `update()` из WS).

Два независимых механизма показывали **одно и то же значение** последней закрытой свечи — отсюда две одинаковые подписи `81 086,84` одна над другой. Это не разные данные.

## J. Исправление графика

Встроенные дубликаты выключены в конфигурации серии свечей: `lastValueVisible: false, priceLineVisible: false`. Осознанная линия осталась единственной — она окрашивается по направлению свечи и обновляется в реальном времени. Это ровно тот же подход, который в файле уже применён к volume (:195-196) и RSI (:222-223).

Генуинно разные линии не удалялись: `createPriceLine` вызывается 2 раза — это один и тот же уровень на двух путях отрисовки (первичная загрузка и WS-обновление), а не две сущности.

## K. Панель фьючерсов

Заголовок остался русским, внутри — нормальный английский: `Open Interest`, `24ч Futures Volume`, `BTC Basis`, `Funding`. **Данные не менялись** — правки только в подписях; это подтверждено ревью diff (см. раздел W).

## L. Ликвидации

Карточка на Overview: `Ликвидации · 24ч`. `Long`/`Short` в английском виде. Значения не менялись.

## M. Тепловая карта

Заголовок `Тепловая карта`. Бейдж `LIVE-ПЛИТКИ` → `LIVE`; в QA-режиме `QA`. Кнопки метрик приведены к одному набору: `24ч Change`, `Volume`, `Open Interest`, `Funding`. Ощущение «двух переводчиков» снято.

## N. Тикер

Реального браузера здесь нет (см. P), поэтому — по коду:
- анимация трека отключается при `prefers-reduced-motion` (`src/index.css:464-466`), общий блок — `:411`;
- метка режима приведена к единой системе (`LIVE` / `QA`) — тест `uxCleanup` обновлён и проходит;
- **сетевых запросов не добавлено** (раздел V).

Проверить отсутствие шва на стыке, горизонтальный скролл body и сброс анимации при live-обновлении **визуально я не смог** — это требует живого браузера.

## O. Sticky-шапка

`<header class="sticky top-0 z-40 …">` (:389) — это `sticky`, а не `fixed`, поэтому контент течёт под шапкой и компенсация высоты не нужна. Дропдауны (`z-50` на :310, :355, :660) выше шапки — выпадающие меню не перекрываются. Тикер находится вне стека шапки.

## P. Реальный браузер / вьюпорты — **НЕ ВЫПОЛНЕНО**

**Это единственный пункт задания, который я не смог сделать, и я его не имитировал.** Скриншотов нет, и цифр по вьюпортам 390×844 / 430×932 / 768×1024 / 1280×720 / 1366×768 / 1440×900 / 1920×1080 я вам не приведу, потому что их не существует.

Что пробовал и почему не вышло:

| Путь | Результат |
|---|---|
| `npx playwright install chromium` | `ECONNRESET cdn.playwright.dev` |
| `@sparticuz/chromium` через npm | ставится (67 МБ, распакован в `/tmp/chromium`) |
| `ldd /tmp/chromium` | не хватает `libnss3`, `libnspr4`, `libnssutil3` |
| apt / Debian / conda / npmmirror | HTTP `000` — недоступны |
| NSS в npm и PyPI | отсутствует |
| самодельные no-op заглушки NSS (gcc, 44 символа, version script) | `ldd` чистый, Chromium стартует, но **висит без рендерера** (таймаут 70 с) с `--headless --no-zygote --single-process` |

**Чем заменил:** структурные инварианты и рендер-тесты. Они ловят причину оверлапа (несжимаемый пункт без усечения) и регрессии найденных багов, но **не заменяют** визуальный прогон по вьюпортам и не покрывают §17 (шов тикера) и §19 (мобильная верстка). Эти два пункта остаются открытыми и требуют прогона на машине с браузером.

## Q. Стратегии

Основной экран — ровно 3, из реестра, а не из брифа: `PRODUCT_STRATEGY_IDS = ['V3_0_HTF_LIQUIDATION_TRAP', 'V3_3_HTF_ZONE_MITIGATION', 'V2_8_ZERO_FEE_SNIPER_TRAILING']`, фильтрация на `ProductStrategiesSection.tsx:138`.

Исследовательский архив — в `Collapsible` с `testId="research-archive-collapsible"`, `mountOnOpen`, **без `defaultOpen`** → свёрнут по умолчанию. 13 карточек не возвращались, исследования/архив/дисклеймер не удалены. Математика стратегий не менялась.

## R. Сигналы

Только презентация; значения **не правились на глаз**. Проблема `ETH/USDT/USDT` в V3.3 осталась как есть — это отдельный этап корректности, вы просили его не трогать в визуальном проходе.

## S. Регистрация

Экран закрытой регистрации существует и управляется **статусом бэкенда**, а не ошибкой формы: `RegisterPage.tsx:42-43` читает `body.registrationOpen` из API, `:103` — `if (registrationOpen === false)` рендерит экран вместо формы. Заголовок приведён к формулировке из ТЗ: `Регистрация закрыта` → **`Регистрация временно закрыта`**, кнопка `Войти` (`/login`). SMTP пользователю не упоминается.

## T. Реклама / пустые слоты

Подключать рекламу не стал — только аудит. `content/sponsor-slots.json` → `"placements": []`. `SponsorSlot.tsx:8`: `if (items.length === 0) return null;` — **до** любого JSX, поэтому не создаётся ни контейнер, ни `space-y-2`, ни отступ. Проверено рендер-тестом: `container.firstChild === null`. Ничего не резервируется.

## U. Доступность

- `prefers-reduced-motion: reduce` — `src/index.css:411` и `:464-466`;
- тултипы источника данных в шапке (`title=`) приведены к новой системе статусов;
- дропдауны на `z-50`, доступны с клавиатуры в существующей реализации (не менял).

**Контраст и размеры тач-таргетов я не измерял** — это требует живого браузера.

## V. Сеть / производительность

**Ни одного нового сетевого запроса не добавлено** — все правки в подписях, классах и конфигурации существующих серий. `createPriceLine` вызывается те же 2 раза, что и до правки.

Code splitting цел — билд даёт отдельные чанки: `StrategiesPage` 554,81 kB, `index` 318,92 kB, `vendor-react` 164,56 kB, `vendor-charts` 162,75 kB, `CoinDetailPage` 61,48 kB, `vendor-ui` 35,91 kB, `LiquidationsPage` 31,21 kB.

Сравнить число запросов на старте **не смог** — нужен живой браузер.

## W. Тесты

Новые — 22:

| Файл | Тестов | Что |
|---|---|---|
| `tests/unit/headerLayoutInvariant.test.ts` | 13 | структурные инварианты шапки |
| `tests/unit/visualQaRegressions.test.ts` | 9 | регрессии на найденные баги |

`headerLayoutInvariant` проверяет: бренд `shrink-0`; контролы `shrink-0` + `ml-auto`; строка `flex-wrap` + `min-w-0`; **ни один** пункт навигации не имеет `shrink-0` вместе с `whitespace-nowrap`; у каждого пункта есть `min-w-0`; обе подписи с `truncate`; `<nav>` с `min-w-0`/`xl:flex-1`/`xl:justify-center`; ≥2 иконок гейтятся `nav2xl`; кегль `text-[13px]` + `2xl:text-sm`; поиск и бейдж версии гейтятся `navxl`; `PRIMARY_NAV_ITEMS.length ≤ PRIMARY_NAV_CAPACITY`; бюджет ширины 1280 px; `Рынок` → `['/market','/futures']` и **ноль** top-level `/futures`.

**Непустотность доказана:** на прежнем коде шапки (бэкап `/tmp/Header.bak`) — **3 падают / 10 проходят**; с фиксом — **13 проходят**.

`visualQaRegressions` покрывает: отсутствие «перп» в UI; 5 карточек при `lg:grid-cols-5` и отсутствие карточки Data Mode; свёрнутый баннер источника; отсутствие дубля цены (`lastValueVisible/priceLineVisible = false` + ровно 2 `createPriceLine`); пустой `SponsorSlot` → `null` без обёртки; экран закрытой регистрации из статуса бэкенда.

Обновлён `tests/unit/uxCleanup.test.tsx` — три assertions на заголовок регистрации и один на метку тикера (они проверяли прежние формулировки).

**Полный прогон: 973 passed / 90 files / 0 unhandled.**

## X. tsc

`npx tsc --noEmit` → **EXIT=0**, чисто. Прогонялся после каждой правки.

## Y. Билд

`npm run build` → **✓ built in 6.36s**, EXIT=0.

## Z. Коммит

**`6dced33`** — `fix(ui): визуальный QA-проход — оверлап шапки, дубль цены, терминология`. Отдельный QA-коммит, как просили. Дерево чистое.

Diff: 16 файлов, +64 / −46. Проверено, что изменённые строки — **только подписи и классы**: отфильтрованный diff (без меток и классов) показывает исключительно текстовые правки (`Изм. 24ч` → `24ч Change`, `Объём` → `Volume`, `Открытый интерес` → `Open Interest`, `Базис BTC` → `BTC Basis`) и `<span>{label}</span>` из обёртки под `truncate`. Данных, формул и сетевых вызовов правки не касаются.

---

## Полная верификация (§27)

| Команда | Результат |
|---|---|
| `rm -rf node_modules && npm ci` | **EXIT=0** (290 пакетов в `node_modules`) |
| `npx tsc --noEmit` | **EXIT=0** |
| `npm test` | **973 passed / 90 files / 0 unhandled** |
| `npm run build` | **✓ 6.36 s, EXIT=0** |
| `npm audit --omit=dev` | **2 moderate**, 0 critical, 0 high |

Аудит: обе moderate — React Router (`GHSA-337j-9hxr-rhxg`, arbitrary constructor injection в `deserializeErrors()` при SSR-гидрации; CVE-2025-68470 bypass). Фикс требует `react-router-dom@7.18.4` — **breaking change**. Не применял: `npm audit fix --force` запрещён, апгрейд на React Router 7 запрещён.

---

## Найдено, но сознательно НЕ исправлено

1. **`ETH/USDT/USDT` в сигнале V3.3** — вы явно отнесли это к отдельному этапу корректности. Не трогал.
2. **React Router 2 moderate** — фикс только через breaking-апгрейд на v7.
3. **§17 шов тикера и §19 мобильная верстка** — не проверено, нужен живой браузер. Не «исправлено вслепую».
4. **Контраст и тач-таргеты (§24)** — не измерялись по той же причине.
5. **Математика стратегий, значения сигналов, данные фьючерсов/ликвидаций** — не менялись по вашему запрету.
6. **Реклама** — не подключалась, только аудит (§23).

## Что осталось открытым

- **§17 и §19** — визуальный прогон по 7 вьюпортам и 5 роутам в реальном браузере. В этой песочнице невыполнимо: Playwright CDN, apt, зеркала Debian/conda и системный NSS недоступны; собранные вручную заглушки NSS дают стартующий, но не рендерящий Chromium.
- **`git push`** — токен GitHub недействителен. Локально 7 коммитов, не отправленных в `origin`. Подключение GitHub в Arena нужно переподключить; учётные данные я не запрашиваю.
